# =============================================
# AI Knowledge Centre — Unified Requirements
# =============================================
# Usage: pip install -r requirements.txt
#
# To install for a specific service only:
#   pip install -r requirements.txt --target <service-dir>
#
# Sections:
#   Core     — shared by all services
#   Auth     — JWT + password hashing
#   Ingestion — document parsing, OCR, embeddings
#   Query    — RAG, vector search, reranking
#   Observability — LLM tracing, metrics, testing
# =============================================

# ── Core ────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
python-multipart==0.0.20
aiofiles==24.1.0

# ── Database ────────────────────────────────
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
redis[hiredis]==5.2.1

# ── Auth / Security ─────────────────────────
PyJWT>=2.8.0
bcrypt==4.2.1

# ── Document Ingestion ──────────────────────
celery[redis]==5.4.0
unstructured[all-docs]==0.16.12
python-docx==1.1.2
openpyxl==3.1.5
pytesseract==0.3.13
pdf2image==1.17.0

# ── Vector Search / Embeddings ──────────────
qdrant-client==1.12.1
sentence-transformers==3.3.1
rank-bm25==0.2.2
numpy==1.26.4

# ── ML / LLM ────────────────────────────────
torch==2.5.1+cpu
transformers==4.47.1

# ── Storage / Object Store ─────────────────
boto3==1.35.86

# ── Observability ───────────────────────────
langfuse==2.58.1
prometheus-client==0.21.1

# ── Testing (dev only) ──────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0

# ── RAGAS Evaluation ──────────────────────
ragas==0.2.0
datasets==3.2.0
pandas==2.2.3
